# Workspace

This folder is the recommended working area for Gangent-generated files.

Start the CLI with:

```powershell
python -m gangent.cli --provider deepseek --workspace-root .\workspace --profile auto --output quiet
```

Use this folder when you want Gangent to act on a dedicated target directory
instead of editing the Gangent repo root.
